# 本机开发可以不填，请求会走 Vite 代理到 localhost:8080
# Render 静态站点构建时必须填写已上线的 Java 地址（不要末尾斜杠）
VITE_API_BASE=https://tradehub-api-bxz2.onrender.com

# 独立站公网地址。上线后改成 Render 域名并重新构建后台
VITE_STOREFRONT_URL=https://tradehub-storefront.onrender.com
